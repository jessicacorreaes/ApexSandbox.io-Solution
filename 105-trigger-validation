public void validateInsert(List<Opportunity> opportunities){
    
    for(Opportunity opp :opportunities ){
        if(opp.StageName == null && String.isBlank(opp.Description)){
        opp.Description.addError('Closed Won DescriptionDescription Description should not be empty for Closed Won opportunity.');
        }
    }
}