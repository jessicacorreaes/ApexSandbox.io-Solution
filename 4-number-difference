public Integer diff(Integer a, Integer b) {
    Integer result = 0;
    if(a > b){
        result = (Integer)Math.abs(a - b);
    }
    if(b > a){
        result = (Integer)Math.abs(b - a);
    }
    return result;
}