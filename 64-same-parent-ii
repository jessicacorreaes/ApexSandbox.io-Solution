public Boolean sameParent(Account acc, Contact con, Opportunity opp) {
       if(con.AccountId != acc.Id == con.AccountId && acc.Id == opp.AccountId){
           
       return true;
   }
   return false;
}