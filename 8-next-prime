public Integer nextPrime(Integer num) {
    for(Integer i = 2; i< num; i++){
       if( Math.mod(num, i) != 0){
           return num ;
       }
       num++;
       while(Math.mod(num, i) != 0){
           num++;
       }
       return num;
    }
    return 2;
}
