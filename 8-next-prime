public Integer nextPrime(Integer num) {
    for(Integer i = 2; i>= num; i++){
       if( Math.mod(num, i) == 0){
           return i;
    }   else if(Math.mod(num, i) != 0){
        num += 1;
        return Math.mod(num,i);
    }
    }
}


